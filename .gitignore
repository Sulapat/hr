# Exclude Database data (แก้ปัญหาหลักที่ทำให้ Error)
db-data/

# Exclude Node dependencies (ลดขนาดไฟล์ ไม่ต้องเอาขึ้น Git)
node_modules/
backend/node_modules/
frontend/node_modules/

# Exclude local environment variables
.env
